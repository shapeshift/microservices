model Notification {
  id          String    @id @default(cuid())
  title       String
  body        String
  type        String
  sentAt      DateTime  @default(now())
  deliveredAt DateTime?
  userId      String
  swapId      String?

  @@map("notifications")
}
