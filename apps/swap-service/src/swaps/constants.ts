export const SHAPESHIFT_BPS = 10
export const REFERRER_FEE_RATE = 0.1
export const PENDING_TIMEOUT_MS = 24 * 60 * 60 * 1000
export const UNREACHABLE_TIMEOUT_MS = 7 * 24 * 60 * 60 * 1000
// any tolerance is a window to claim an already-mined tx, so it stays 0 until shadow data argues otherwise
export const ATTRIBUTION_TOLERANCE_MS = 0
export const ATTRIBUTION_RETRY_MS = 15 * 60 * 1000
